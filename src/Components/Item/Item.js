import React from 'react';

const Item = ({item}) => {
    console.log(item);
    // const {name}=item;
    return (
        <div>
            {/* <h2>{item.name}</h2> */}
        </div>
    );
};

export default Item;